---
description: Architecture
---

SDC follows a modular architecture.

Never tightly couple features.

Prefer reusable services.

Business logic belongs outside UI.

Avoid duplication.

Prefer composition over inheritance.

Never create circular dependencies.

Prefer feature modules.

Every feature must be independently testable.